// Here is where the javascript code that handles rendering a calender for calendar.html is put

// The general layout (7 days and 6 weeks) is already in place in the html file

// Need to adjust the date info (overall month/year and numbering the dates in a week)

// Also dependent on the current date, need to query the database for the appriopriate range

// As iterate through the info, append (up to a limit) a number of tasks on a given date